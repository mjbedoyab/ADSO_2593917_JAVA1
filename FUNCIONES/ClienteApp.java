import java.util.Scanner;

public class ClienteApp {
    public static void main(String[] args) {
        
    }
    
}
// // Definir un arreglo de objetos de tipo String
// String[] arr = {"Uno", "Dos", "Tres", "Cuatro", "Cinco"};

// // Identificar el índice del objeto que deseas eliminar
// int indiceObjetoAEliminar = 2;

// // Crear un nuevo arreglo con una longitud menor que el arreglo original
// String[] nuevoArr = new String[arr.length - 1];

// // Copiar todos los objetos del arreglo original al nuevo arreglo excepto el objeto que deseas eliminar
// int contador = 0;
// for (int i = 0; i < arr.length; i++) {
//     if (i != indiceObjetoAEliminar) {
//         nuevoArr[contador] = arr[i];
//         contador++;
//     }
// }

// // Asignar el nuevo arreglo al arreglo original
// arr = nuevoArr;
